# internpe-diabetes-prediction
This project predicts whether a person has diabetes or not using Machine Learning. The model is trained on the PIMA Indians Diabetes Dataset and uses the Support Vector Machine (SVM) algorithm for classification.
